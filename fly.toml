# fly.toml app configuration file generated for redirectfy on 2024-04-21T23:29:35-04:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'redirectfy'
primary_region = 'gru'

[build]
  dockerfile = 'Dockerfile.prod'

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
