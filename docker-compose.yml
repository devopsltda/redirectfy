version: "3.8"

services:
  app:
    build:
      context: ./
      target: env
    volumes:
      - ./:/app
    ports:
      - "8080:8080"
    command: sh -c "npm install && go get ./... && make watch"
